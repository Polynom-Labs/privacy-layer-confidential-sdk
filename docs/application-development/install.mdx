---
title: Install
description: Install the Stellar privacy client and Redux state adapter
---

Add `@arcanetech/privacy-sdk-stellar` and `@arcanetech/privacy-sdk-state-redux` to your React application. The Redux adapter also requires `@reduxjs/toolkit` as a peer dependency.

Node.js 20.10 or later is enough to install. `@stellar/stellar-sdk` 16 (pulled by the Stellar preset) declares Node 22+; install on 20.10 may print `EBADENGINE`.

For a runnable browser demo first, see [Quick start](/integration/quick-start). Scripts and services without React should use [`@arcanetech/privacy-sdk-state-memory`](https://www.npmjs.com/package/@arcanetech/privacy-sdk-state-memory) — see [Quick start](/integration/quick-start).

<CodeGroup>

```bash npm
npm install @arcanetech/privacy-sdk-stellar @arcanetech/privacy-sdk-state-redux @reduxjs/toolkit react-redux
```

```bash yarn
yarn add @arcanetech/privacy-sdk-stellar @arcanetech/privacy-sdk-state-redux @reduxjs/toolkit react-redux
```

```bash pnpm
pnpm add @arcanetech/privacy-sdk-stellar @arcanetech/privacy-sdk-state-redux @reduxjs/toolkit react-redux
```

</CodeGroup>

For **private sender** (zero public deposit) and **escrow sends** to unregistered recipients, also install [`@arcanetech/privacy-sdk-relay`](https://www.npmjs.com/package/@arcanetech/privacy-sdk-relay). `operation.execute()` is Direct Submission only and does not contact the relayer.

<CodeGroup>

```bash npm
npm install @arcanetech/privacy-sdk-relay
```

```bash yarn
yarn add @arcanetech/privacy-sdk-relay
```

```bash pnpm
pnpm add @arcanetech/privacy-sdk-relay
```

</CodeGroup>

Stand values (pool, registry, KYT, application id, audit key, Relayer origin) are not in these install lines — see [Getting access](/integration/getting-access).

Relay wiring after prepare: [Quick start](/integration/quick-start), and [Withdraw](/application-development/how-to/withdraw).

Next, wire the client in your app shell. See [Setup](./setup).

## Related

<CardGroup cols={2}>
  <Card title="Getting access" icon="key" href="/integration/getting-access">
    Stand values and npm install.
  </Card>
  <Card title="Packages" icon="box" href="/overview/packages">
    Map of `@arcanetech/*` packages and the Stellar preset.
  </Card>
  <Card title="Quick start" icon="rocket" href="/integration/quick-start">
    Frontend or Backend demo tabs.
  </Card>
</CardGroup>
