---
title: Disclosure policy
description: Control on-chain visibility of sender, recipient, asset, and amount for each operation route
---

import { OperationDisclosures } from '../snippets/operation-disclosures.jsx';

**Disclosure** sets which attributes appear in the public, on-chain portion of a private operation. Auditors still receive encrypted audit payloads. Disclosure decides what is also visible as plaintext.

## What disclosure controls

Each operation intent includes a `disclosure` object with four fields:

| Field | Controls visibility of |
| --- | --- |
| `senderAddress` | Who sent the value |
| `recipientAddress` | Who receives the value |
| `assetAddress` | Which asset moved |
| `amount` | How much moved |

Each field is either `'public'` or `'private'`.

## Public vs private

| Value | Effect |
| --- | --- |
| `public` | Attribute included in publicly visible transaction metadata for this operation |
| `private` | Attribute not published in plaintext; auditors rely on encrypted audit data |

Set disclosure on the intent. Each route only accepts some combinations.

## "Both" in route reference tables

Some reference tables in this documentation show **both** for a field (for example withdraw sender). **Both is not a third API value** — it means the route **accepts either** `public` or `private` for that field.

**Private sender** is valid only with **Protocol Relay** (zero public deposit). Public sender uses Direct Submission (`operation.execute()`). Deposit sender cannot be private.

The Stellar **policy adapter** rejects invalid combinations with `unsupported_disclosure` during prepare.

## Route constraints

| Route | Constraint |
| --- | --- |
| Deposit | Recipient is typically `private`; sender/asset/amount typically `public`. Relayer does not apply. |
| Registered private transfer | Recipient/asset/amount typically `private`. Sender may be public or private (private requires Protocol Relay). |
| Escrow send to unregistered recipient | Same private disclosure as a registered transfer. Sender may be public or private (private requires Protocol Relay). Escrow send itself requires Protocol Relay. |
| Withdraw to a public wallet | Recipient, asset, and amount must be `public`. Sender may be public or private (private requires Protocol Relay). |

Set a valid disclosure before you call prepare. Execute does not fix an invalid combination.

## Per-operation reference

### Deposit

<OperationDisclosures operation="deposit" />

Sender, asset, and amount are public. Recipient stays private because the deposit creates a private pool note.

### Withdraw

<OperationDisclosures operation="withdraw" />

Sender may be public or private. Payout to a public wallet requires recipient, asset, and amount to be public. Private sender requires Protocol Relay.

### Transfer to registered recipient

<OperationDisclosures operation="transfer-registered" />

Sender may be public or private. Recipient, asset, and amount stay private for fully private transfers between registered users. Private sender requires Protocol Relay.

### Transfer to unregistered recipient

<OperationDisclosures operation="transfer-unregistered" />

Transfers to an unregistered recipient use the same private disclosure as a registered transfer (the documented product path). The escrow note owner is derived from a nonce and the recipient G-address; nothing about the recipient is published on-chain. Escrow sends require Protocol Relay. Private sender also requires Protocol Relay. Policy also accepts a fully public disclosure for a public `G…` recipient; demos and how-tos use the private escrow path.

## Choosing disclosure in your product

- **More public fields** — simpler reconciliation, clearer compliance signals, less privacy on those attributes.
- **More private fields** — stronger privacy; ensure your audit and KYT configuration supports the route.

Read [Security and privacy](/concepts/security-and-privacy) for where disclosed fields appear relative to encrypted audit data.

## Related

<CardGroup cols={2}>
  <Card title="Operations" icon="arrow-right-left" href="/concepts/operations">
    How each operation uses disclosure.
  </Card>
  <Card title="Security and privacy" icon="shield" href="/concepts/security-and-privacy">
    Local storage vs on-chain visibility.
  </Card>
  <Card title="Operation lifecycle" icon="history" href="/concepts/operation-lifecycle">
    When policy errors surface.
  </Card>
</CardGroup>
